---
- name: "Converge"
  hosts: "all"
  gather_facts: true # Disable if your role does not rely on facts

  pre_tasks:
    - name: "Update DNF cache"
      ansible.builtin.dnf:
        update_cache: true
        state: "present"

  roles:
    - role: "goathy.technitium_dns"
